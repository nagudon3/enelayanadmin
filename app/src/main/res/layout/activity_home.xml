<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:background="#FFFFFF"
    android:padding="10dp"
    android:layout_height="match_parent"
    tools:context=".Activity.HomeActivity">

    <ImageView
        android:id="@+id/enelayanLogo"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_marginTop="28dp"
        android:background="@drawable/logo"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.498"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:gravity="center"
        android:text="e-Nelayan Admin"
        android:textColor="@color/colorPrimary"
        android:textSize="30dp"
        android:id="@+id/title"
        app:layout_constraintTop_toBottomOf="@+id/enelayanLogo"
        tools:layout_editor_absoluteX="0dp" />

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Check approval for: "
        android:gravity="center"
        android:textColor="@color/colorPrimary"
        android:textSize="20dp"
        android:id="@+id/approvalTag"
        android:layout_marginTop="10dp"
        app:layout_constraintTop_toBottomOf="@+id/title"/>

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/fishermanButton"
        android:background="@drawable/corner_button"
        android:textColor="@color/colorText"
        android:text="Fisherman"
        app:layout_constraintTop_toBottomOf="@id/approvalTag"
        android:textSize="20dp"
        android:layout_marginTop="20dp"
        android:textAllCaps="false"
        />

    <Button
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:background="@drawable/corner_button"
        android:textColor="@color/colorText"
        android:id="@+id/sellerButton"
        android:text="Seller"
        android:textAllCaps="false"
        app:layout_constraintTop_toBottomOf="@id/fishermanButton"
        android:textSize="20dp"
        />

    <Button
        android:id="@+id/logoutBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginBottom="40dp"
        android:text="Logout"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:textColor="@color/colorText"
        android:background="@drawable/corner_button"
        app:layout_constraintTop_toBottomOf="@+id/sellerButton"
        app:layout_constraintVertical_bias="0.235" />
</androidx.constraintlayout.widget.ConstraintLayout>